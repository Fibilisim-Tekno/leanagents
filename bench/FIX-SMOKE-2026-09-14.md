# Automatic fix smoke test — 2026-09-14

Local example: examples/batch-fix. Known off-by-one defect in the exclusive
array slice endpoint. This is a constructed smoke test, not held-out evidence.

CLI: Codex 0.153.4, gpt-6-astra, low effort, existing ChatGPT authentication.
The host supplied explicit TypeScript source and immutable Node tests.

1. Baseline: 3 tests, 1 passed and 2 failed.
2. Implementation call changed `i + size - 1` to `i + size`.
3. Candidate: all 3 tests passed. Selected test contents were unchanged.
4. Separate reviewer returned approve with no findings.
5. `apply` applied the verified change to a fresh copy of the original example.
6. Post-application Node test run passed all 3 tests.

| Stage | Input tokens | Cached input (included) | Output tokens |
| --- | --- | --- | --- |
| Implementation | 15,769 | 11,776 | 247 |
| Review | 16,610 | 11,776 | 14 |

Exactly two model calls; no model tool calls. Total input plus output: 32,640.
No cost or ECC savings comparison was performed. Correctness beyond these
tests and the reviewer remains unproven.

Local raw evidence: outputs/leanagents-audit/fix-batch-v1 and
fix-batch-apply-v1 in the workspace used to run this experiment. The deliberately
broken distributed example was preserved.
