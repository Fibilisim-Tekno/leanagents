---
name: some-other-name
description: The frontmatter name does not match the filename, which breaks predictable lookups at build time.
---

Body text so the empty-body rule does not also fire here.
